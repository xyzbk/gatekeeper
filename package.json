{
  "name": "gatekeeper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first repository intelligence for Codex.",
  "license": "MIT",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "build": "tsc -b --pretty false --force && pnpm --filter @gatekeeper/dashboard build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "fixtures:prepare": "tsx demo/prepare-fixtures.ts",
    "lint": "eslint packages apps demo --max-warnings 0",
    "schema:verdict": "pnpm --filter @gatekeeper/contracts schema:verdict",
    "test": "vitest run --config vitest.workspace.ts",
    "typecheck": "tsc -b --pretty false && pnpm --filter @gatekeeper/dashboard typecheck"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.3",
    "eslint": "10.7.0",
    "prettier": "3.9.5",
    "tsx": "4.23.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vite": "8.1.4",
    "vitest": "4.1.10"
  }
}
